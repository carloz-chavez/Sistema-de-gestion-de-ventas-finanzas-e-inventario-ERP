import { annulSalePayment } from "@/app/actions";
type Row=Record<string,unknown>;const money=(v:unknown)=>`S/ ${Number(v||0).toFixed(2)}`;
export function PaymentHistory({rows}:{rows:Row[]}){const payments=rows.filter(r=>r.kind==="payment");if(!payments.length)return null;return <section className="panel paymentHistory"><div className="panelHead"><div><h2>Cobros registrados</h2><p>Puedes anular un cobro incorrecto; la venta recuperará automáticamente su saldo.</p></div></div><div className="tableWrap"><table><thead><tr><th>Venta</th><th>Fecha</th><th>Medio</th><th>Monto</th><th></th></tr></thead><tbody>{payments.map(p=><tr key={String(p.id)}><td>{String((p.venta as Row)?.codigo||"")}</td><td>{new Date(String(p.fecha_pago)).toLocaleDateString("es-PE")}</td><td>{String(p.medio_pago)}</td><td>{money(p.monto)}</td><td><form action={annulSalePayment}><input type="hidden" name="id" value={String(p.id)}/><button className="miniDanger">Anular</button></form></td></tr>)}</tbody></table></div></section>}

