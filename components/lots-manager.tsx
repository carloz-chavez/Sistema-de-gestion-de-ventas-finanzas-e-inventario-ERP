import { addProductionLabor, addProductionMaterial, createLot, finishProduction, startProduction } from "@/app/actions";
type Choice={id:number;nombre:string};type Variety=Choice&{producto:{nombre:string}|null};type Production={id:number;estado:string;cantidad_enjavada:number|null;costo_total_javas:number|null;detalle_produccion_material:{costo_total:number}[];detalle_mano_obra:{costo_total:number}[]};type Lot={id:number;codigo:string;fecha_registro:string;cantidad_estimada_javas:number|null;estado:string;productor:{nombre:string}|null;variedad:(Variety&{producto:{nombre:string}|null})|null;produccion:Production|Production[]|null};
export function LotsManager({lots,producers,varieties,materials,workers}:{lots:Lot[];producers:Choice[];varieties:Variety[];materials:Choice[];workers:Choice[]}){return <div className="productionPage"><div className="masterLayout lotLayout"><section className="panel formPanel"><h2>Registrar lote</h2><form action={createLot} className="masterForm"><label>Productor<select name="productor_id" required defaultValue=""><option value="" disabled>Seleccionar productor</option>{producers.map(x=><option key={x.id} value={x.id}>{x.nombre}</option>)}</select></label><label>Producto y variedad<select name="variedad_id" required defaultValue=""><option value="" disabled>Seleccionar variedad</option>{varieties.map(x=><option key={x.id} value={x.id}>{x.producto?.nombre} — {x.nombre}</option>)}</select></label><label>Fecha<input name="fecha_registro" type="date" required defaultValue={new Date().toISOString().slice(0,10)}/></label><label>Javas estimadas<input name="cantidad_estimada_javas" type="number" min="1"/></label><label>Observaciones<textarea name="observaciones" rows={3}/></label><button className="primary">Crear lote</button></form></section><section className="lotCards">{lots.map(l=><LotCard key={l.id} lot={l} materials={materials} workers={workers}/>)}</section></div></div>}
function LotCard({lot,materials,workers}:{lot:Lot;materials:Choice[];workers:Choice[]}){const production=Array.isArray(lot.produccion)?lot.produccion[0]:lot.produccion;const materialCost=production?.detalle_produccion_material?.reduce((a,x)=>a+Number(x.costo_total),0)||0;const laborCost=production?.detalle_mano_obra?.reduce((a,x)=>a+Number(x.costo_total),0)||0;return <article className="panel lotCard"><div className="lotHead"><div><p className="eyebrow">{lot.codigo}</p><h2>{lot.variedad?.producto?.nombre} — {lot.variedad?.nombre}</h2><p>{lot.productor?.nombre} · {lot.fecha_registro} · {lot.cantidad_estimada_javas||"—"} javas estimadas</p></div><span className="status">{lot.estado.replaceAll("_"," ")}</span></div>{!production?<form action={startProduction} className="lotAction"><input type="hidden" name="lote_id" value={lot.id}/><button className="primary">Iniciar producción</button></form>:production.estado==="TERMINADA"?<div className="productionSummary"><span>Enjavadas <strong>{production.cantidad_enjavada}</strong></span><span>Javas <strong>S/ {production.costo_total_javas||0}</strong></span><span>Materiales <strong>S/ {materialCost.toFixed(2)}</strong></span><span>Mano de obra <strong>S/ {laborCost.toFixed(2)}</strong></span></div>:<div className="productionForms"><details><summary>Agregar material</summary><form action={addProductionMaterial} className="inlineForm"><input type="hidden" name="produccion_id" value={production.id}/><select name="material_id" required defaultValue=""><option value="" disabled>Material</option>{materials.map(x=><option key={x.id} value={x.id}>{x.nombre}</option>)}</select><input name="cantidad" type="number" min="0.001" step="0.001" placeholder="Cantidad" required/><input name="costo_unitario" type="number" min="0" step="0.0001" placeholder="Costo unitario" required/><button className="secondary">Guardar</button></form></details><details><summary>Agregar mano de obra</summary><form action={addProductionLabor} className="inlineForm"><input type="hidden" name="produccion_id" value={production.id}/><select name="trabajador_id" required defaultValue=""><option value="" disabled>Trabajador</option>{workers.map(x=><option key={x.id} value={x.id}>{x.nombre}</option>)}</select><select name="tipo_pago" required defaultValue="JORNADA"><option>JORNADA</option><option>JAVA</option><option>DIA</option><option>OTRO</option></select><input name="cantidad" type="number" min="0.001" step="0.001" placeholder="Cantidad" required/><input name="costo_unitario" type="number" min="0" step="0.0001" placeholder="Costo" required/><button className="secondary">Guardar</button></form></details><details><summary>Finalizar producción</summary><form action={finishProduction} className="inlineForm"><input type="hidden" name="produccion_id" value={production.id}/><input type="hidden" name="lote_id" value={lot.id}/><input name="cantidad_enjavada" type="number" min="1" placeholder="Cantidad enjavada" required/><input name="cantidad_javas_utilizadas" type="number" min="1" placeholder="Javas utilizadas" required/><input name="costo_java_unitario" type="number" min="0" step="0.0001" placeholder="Costo por java" required/><button className="primary">Finalizar</button></form></details></div>}</article>}

