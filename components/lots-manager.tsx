import { createLot } from "@/app/actions";
type Choice={id:number;nombre:string};type Variety=Choice&{producto:{nombre:string}|null};type Lot={id:number;codigo:string;fecha_registro:string;cantidad_estimada_javas:number|null;estado:string;productor:{nombre:string}|null;variedad:(Variety&{producto:{nombre:string}|null})|null};
export function LotsManager({lots,producers,varieties}:{lots:Lot[];producers:Choice[];varieties:Variety[]}){return <div className="masterLayout lotLayout"><section className="panel formPanel"><h2>Registrar lote</h2><form action={createLot} className="masterForm"><label>Productor<select name="productor_id" required defaultValue=""><option value="" disabled>Seleccionar productor</option>{producers.map(x=><option key={x.id} value={x.id}>{x.nombre}</option>)}</select></label><label>Producto y variedad<select name="variedad_id" required defaultValue=""><option value="" disabled>Seleccionar variedad</option>{varieties.map(x=><option key={x.id} value={x.id}>{x.producto?.nombre} — {x.nombre}</option>)}</select></label><label>Fecha de registro<input name="fecha_registro" type="date" required defaultValue={new Date().toISOString().slice(0,10)}/></label><label>Cantidad estimada de javas<input name="cantidad_estimada_javas" type="number" min="1"/></label><label>Observaciones<textarea name="observaciones" rows={3}/></label><button className="primary" type="submit">Crear lote</button></form></section><section className="panel"><div className="panelHead"><div><h2>Lotes registrados</h2><p>{lots.length} lotes</p></div></div><div className="tableWrap"><table><thead><tr><th>Código</th><th>Productor</th><th>Producto</th><th>Fecha</th><th>Javas</th><th>Estado</th></tr></thead><tbody>{lots.map(l=><tr key={l.id}><td><strong>{l.codigo}</strong></td><td>{l.productor?.nombre}</td><td>{l.variedad?.producto?.nombre} — {l.variedad?.nombre}</td><td>{l.fecha_registro}</td><td>{l.cantidad_estimada_javas||"—"}</td><td><span className="status">{l.estado.replaceAll("_"," ")}</span></td></tr>)}</tbody></table></div></section></div>}

